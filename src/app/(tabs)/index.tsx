import { Text, View } from 'react-native';

// Placeholder — home screen (clock + week grid) is built in the next prompt.
export default function HomeScreen() {
  return (
    <View
      style={{ flex: 1, backgroundColor: '#0c0c0c', alignItems: 'center', justifyContent: 'center' }}
    >
      <Text style={{ fontFamily: 'Courier', fontSize: 14, color: '#333' }}>
        home screen — coming next
      </Text>
    </View>
  );
}
